[project]
name = "youtube-clone"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.6",
    "drf-spectacular>=0.30.0",
    "imagekitio>=5.7.0",
    "python-dotenv>=1.2.2",
]
